<!DOCTYPE html>
<html lang="<%= locale %>">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= __('enter_details_title') %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
      <h1 class="text-2xl font-bold text-center text-blue-600 mb-6">
        <%= __('enter_details_title') %>
      </h1>
      <form
        action="/submit-details"
        method="POST"
        enctype="multipart/form-data"
        class="space-y-4"
      >
        <div>
          <label
            for="teacherName"
            class="block text-sm font-medium text-gray-700"
          >
            <%= __('teacher_name') %>
          </label>
          <input
            type="text"
            id="teacherName"
            name="teacherName"
            placeholder="<%= __('teacher_name_placeholder') %>"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300"
          />
        </div>
        <div class="flex gap-4">
          <div class="flex-1">
            <label for="date" class="block text-sm font-medium text-gray-700">
              <%= __('date') %>
            </label>
            <input
              type="date"
              id="date"
              name="date"
              required
              class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300"
            />
          </div>
          <div class="flex-1">
            <label for="time" class="block text-sm font-medium text-gray-700">
              <%= __('time') %>
            </label>
            <input
              type="time"
              id="time"
              name="time"
              required
              class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300"
            />
          </div>
        </div>
        <div>
          <label for="address" class="block text-sm font-medium text-gray-700">
            <%= __('address') %>
          </label>
          <input
            type="text"
            id="address"
            name="address"
            placeholder="<%= __('address_placeholder') %>"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300"
          />
        </div>
        <div>
          <label
            for="studentsPresent"
            class="block text-sm font-medium text-gray-700"
          >
            <%= __('students_present') %>
          </label>
          <input
            type="number"
            id="studentsPresent"
            name="studentsPresent"
            placeholder="<%= __('students_present_placeholder') %>"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300"
          />
        </div>
        <div>
          <label for="activity" class="block text-sm font-medium text-gray-700">
            <%= __('activity') %>
          </label>
          <textarea
            id="activity"
            name="activity"
            placeholder="<%= __('activity_placeholder') %>"
            rows="4"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300"
          ></textarea>
        </div>
        <div>
          <label
            for="entryImage"
            class="block text-sm font-medium text-gray-700"
          >
            <%= __('entry_image') %>
          </label>
          <input
            type="file"
            id="entryImage"
            name="entryImage"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-md"
          />
        </div>
        <div>
          <label
            for="exitImage"
            class="block text-sm font-medium text-gray-700"
          >
            <%= __('exit_image') %>
          </label>
          <input
            type="file"
            id="exitImage"
            name="exitImage"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-md"
          />
        </div>
        <button
          type="submit"
          class="w-full bg-blue-600 text-white py-2 rounded-md hover:bg-blue-700 transition"
        >
          <%= __('submit') %>
        </button>
      </form>
    </div>
  </body>
</html>
