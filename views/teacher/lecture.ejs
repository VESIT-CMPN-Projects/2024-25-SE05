<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Enter Today's Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
      <h1 class="text-2xl font-bold text-center text-blue-600 mb-6">
        Enter Today's Details
      </h1>
      <form
        action="/teacher/lecture"
        method="POST"
        enctype="multipart/form-data"
        class="space-y-4"
      >
        <div class="flex items-center justify-between mb-6">
          <div class="text-lg font-medium text-gray-700">
            Logged in as: <span class="text-blue-600"><%= username %></span>
          </div>
          <div>
            <img
              src=<%= path %>
              alt="Profile Picture"
              class="w-32 h-32 rounded-full border border-gray-300"
            />
          </div>
        </div>
        <div class="flex gap-4">
          <div class="flex-1">
            <label for="date" class="block text-sm font-medium text-gray-700"
              >Date</label
            >
            <input
              type="date"
              id="date"
              name="date"
              required
              class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300"
            />
          </div>
          <div class="flex-1">
            <label for="time" class="block text-sm font-medium text-gray-700"
              >Time</label
            >
            <input
              type="time"
              id="time"
              name="time"
              required
              class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300"
            />
          </div>
        </div>
        <div>
          <label for="address" class="block text-sm font-medium text-gray-700"
            >Address</label
          >
          <input
            type="text"
            id="address"
            name="address"
            placeholder="Enter the location of vidyapeeth"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300"
          />
        </div>
        <div>
          <label
            for="attendance"
            class="block text-sm font-medium text-gray-700"
            >No. of Students Present</label
          >
          <input
            type="number"
            id="attendance"
            name="attendance"
            placeholder="Enter the number of students present"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300"
          />
        </div>
        <div>
          <label for="activity" class="block text-sm font-medium text-gray-700"
            >Activity</label
          >
          <textarea
            id="activity"
            name="activity"
            placeholder="Enter the activities performed today"
            rows="4"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300"
          ></textarea>
        </div>
        <div>
          <label
            for="entryImage"
            class="block text-sm font-medium text-gray-700"
            >Entry Image</label
          >
          <input
            type="file"
            id="entryImage"
            name="entryImage"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-md"
          />
        </div>
        <div>
          <label for="exitImage" class="block text-sm font-medium text-gray-700"
            >Exit Image</label
          >
          <input
            type="file"
            id="exitImage"
            name="exitImage"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-md"
          />
        </div>
        <button
          type="submit"
          class="w-full bg-blue-600 text-white py-2 rounded-md hover:bg-blue-700 transition"
        >
          Submit
        </button>
      </form>
    </div>
  </body>
</html>
